enum ProductType {
  BOUQUET       @map("bouquet")
  SINGLE_FLOWER @map("single_flower")
}

model Product {
  id String @id @default(uuid(4))

  description String
  imagePath   String
  title       String
  type        ProductType

  offers ShopProduct[]
  favorites Favorite[]

  createdAt DateTime @default(now())
}
