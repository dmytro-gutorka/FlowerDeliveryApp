model Shop {
  id String @id @default(uuid(4))

  name    String
  address String
  lat     String
  lan     String
  imagePath String

  offers ShopProduct[]
  orders Order[]

  createdAt DateTime @default(now())
}
